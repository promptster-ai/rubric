{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://promptster.ai/schema/rubric.schema.json",
  "title": "Promptster AI-fluency rubric",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "phases",
    "tiers",
    "dimensions",
    "methodologySummary",
    "methodologyGroups"
  ],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "phases": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/phase" }
    },
    "tiers": {
      "type": "array",
      "$comment": "Exactly the five tierKey values, each present once. maxItems + a contains per key forbids both omissions (a missing key fails its contains) and duplicates (a dupe leaves no room for the omitted key under maxItems).",
      "minItems": 5,
      "maxItems": 5,
      "items": { "$ref": "#/$defs/tier" },
      "allOf": [
        { "contains": { "properties": { "key": { "const": "strong" } } } },
        { "contains": { "properties": { "key": { "const": "adequate" } } } },
        { "contains": { "properties": { "key": { "const": "developing" } } } },
        { "contains": { "properties": { "key": { "const": "insufficient_evidence" } } } },
        { "contains": { "properties": { "key": { "const": "not_collectible" } } } }
      ]
    },
    "dimensions": {
      "type": "array",
      "$comment": "Exactly the eight dimensionKey values, each present once (same maxItems + per-key contains technique as tiers).",
      "minItems": 8,
      "maxItems": 8,
      "items": { "$ref": "#/$defs/dimension" },
      "allOf": [
        { "contains": { "properties": { "key": { "const": "task_framing" } } } },
        { "contains": { "properties": { "key": { "const": "comprehension_grounding" } } } },
        { "contains": { "properties": { "key": { "const": "direction_quality" } } } },
        { "contains": { "properties": { "key": { "const": "steering_discernment" } } } },
        { "contains": { "properties": { "key": { "const": "verification_loop" } } } },
        { "contains": { "properties": { "key": { "const": "fix_integrity" } } } },
        { "contains": { "properties": { "key": { "const": "context_management" } } } },
        { "contains": { "properties": { "key": { "const": "ecosystem_leverage" } } } }
      ]
    },
    "methodologySummary": { "type": "string", "minLength": 1 },
    "methodologyGroups": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/methodologyGroup" }
    }
  },
  "$defs": {
    "phaseKey": {
      "type": "string",
      "enum": ["discovery", "implementation", "verification", "cross_cutting"]
    },
    "tierKey": {
      "type": "string",
      "enum": [
        "strong",
        "adequate",
        "developing",
        "insufficient_evidence",
        "not_collectible"
      ]
    },
    "surface": {
      "type": "string",
      "enum": ["hiring", "teams"]
    },
    "dimensionKey": {
      "type": "string",
      "enum": [
        "task_framing",
        "comprehension_grounding",
        "direction_quality",
        "steering_discernment",
        "verification_loop",
        "fix_integrity",
        "context_management",
        "ecosystem_leverage"
      ]
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "detail", "url"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "detail": { "type": "string", "minLength": 1 },
        "url": { "type": "string", "format": "uri" }
      }
    },
    "phase": {
      "type": "object",
      "additionalProperties": false,
      "required": ["key", "label", "blurb"],
      "properties": {
        "key": { "$ref": "#/$defs/phaseKey" },
        "label": { "type": "string", "minLength": 1 },
        "blurb": { "type": "string", "minLength": 1 }
      }
    },
    "tier": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "key",
        "label",
        "shortLabel",
        "intent",
        "hex",
        "text",
        "badge",
        "rank",
        "help",
        "structural"
      ],
      "properties": {
        "key": { "$ref": "#/$defs/tierKey" },
        "label": { "type": "string", "minLength": 1 },
        "shortLabel": { "type": "string", "minLength": 1 },
        "intent": { "type": "string", "enum": ["good", "gap", "neutral"] },
        "hex": { "type": "string", "minLength": 1 },
        "text": { "type": "string", "minLength": 1 },
        "badge": {
          "type": "object",
          "additionalProperties": false,
          "required": ["color", "background", "border"],
          "properties": {
            "color": { "type": "string", "minLength": 1 },
            "background": { "type": "string", "minLength": 1 },
            "border": { "type": "string", "minLength": 1 }
          }
        },
        "rank": { "type": "integer", "minimum": 0 },
        "help": { "type": "string", "minLength": 1 },
        "structural": { "type": "boolean" }
      }
    },
    "dimension": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "key",
        "label",
        "phase",
        "surfaces",
        "measures",
        "detail",
        "anchors",
        "sources"
      ],
      "properties": {
        "key": { "$ref": "#/$defs/dimensionKey" },
        "label": { "type": "string", "minLength": 1 },
        "phase": { "$ref": "#/$defs/phaseKey" },
        "surfaces": {
          "type": "array",
          "$comment": "Product surfaces this dimension is judged on. A dimension absent from a surface is omitted from judging there entirely — different from not_collectible, which is a tool limit within a surface.",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/surface" }
        },
        "measures": { "type": "string", "minLength": 1 },
        "detail": { "type": "string", "minLength": 1 },
        "anchors": {
          "type": "object",
          "additionalProperties": false,
          "required": ["developing", "adequate", "strong"],
          "properties": {
            "developing": { "type": "string", "minLength": 1 },
            "adequate": { "type": "string", "minLength": 1 },
            "strong": { "type": "string", "minLength": 1 }
          }
        },
        "sources": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/source" }
        }
      }
    },
    "methodologyGroup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["label", "sources"],
      "properties": {
        "label": { "type": "string", "minLength": 1 },
        "sources": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/source" }
        }
      }
    }
  }
}
